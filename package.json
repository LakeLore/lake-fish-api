{
  "name": "lake-fish-mobile-server",
  "version": "1.0.0",
  "description": "Unified API server for Lake Fish Finder mobile app — all 5 states on one port",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/smoke.js"
  },
  "dependencies": {
    "@sentry/node": "^10.52.0",
    "appattest-checker-node": "^1.0.3",
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.1"
  }
}
